# 10-Fix Summary — Adversarial Benchmark + Cognitive OS Session (June 19, 2026)

Complete list of all fixes applied during the adversarial benchmark, cross-session development, and Cognitive OS building session. All verified post-`/reset`.

## Fixes 1-7 (from FASE 1 adversarial benchmarking)

| # | Tool/Feature | Root Cause | Fix Location | Status |
|---|-------------|------------|--------------|--------|
| 1 | `thought_contradiction` recall 0% | Spanish-only sentiment lexicon; thresholds too strict | `server.py:310` (lexicon), `server.py:855` (thresholds) | ✅ Verified |
| 2 | `check_assumption` schema | Plugin used `id`/`status`, server expects `assumption_id`/`outcome` | `__init__.py:719` | ✅ Verified |
| 3 | `check_assumption` type mismatch | `a["id"]` (int) vs `args["assumption_id"]` (str) → `1 == "1"` = False | `server.py:1252` (`int()` cast) | ✅ Verified |
| 4 | `check_assumption` ID collision | `_next_assumption_id` reset to 1 after `/reset`, colliding with persisted IDs | `server.py:183` (recompute max_id+1) | ✅ Verified |
| 5 | `model_add` missing `properties` | Server accepted it but schema didn't expose it | `server.py:1335`, `__init__.py:728` | ✅ Verified |
| 6 | `model_query` empty results | Schema defined `entity` param, server expects `query` (natural language) | `__init__.py:764` | ✅ Verified |
| 7 | Mental Model 0 dependencies | `_load_state` didn't restore `_next_assumption_id` | `server.py:183` (merged with fix #4) | ✅ Verified |

## Fixes 8-10 (from FASE B-C Cross-Session + Cognitive OS)

| # | Tool/Feature | Root Cause | Fix Location | Status |
|---|-------------|------------|--------------|--------|
| 8 | `agent_message` label resolution | Messages sent to session_id but inbox matched by label | `server.py:2060` (resolve label), `server.py:2079` (match by both) | ✅ Verified |
| 9 | `false` vs `False` in TOOLS dict | Python dict evaluates `false` as NameError at import | `server.py:669` | ✅ Fixed at import |
| 10 | `replace_all=true` HTTP handler corruption | Same string in tool function AND HTTP handler with different indentation | Manual fix in `server.py` | ✅ Fixed |

## New Features (not fixes)

- **Global pattern store** (`_global_patterns`): Patterns shared across sessions
- **`collision_check` tool**: Proactive file conflict detection (5-min window)
- **`session_list` collision warnings**: Shows ⚠️ when multiple sessions touch same file
- **Wiki CRUD via Dashboard**: Full Alpine.js-based entity editor
- **`agent_message`/`agent_inbox`**: Cross-session messaging with label resolution
- **HTTP `/model` CRUD**: GET/POST with `_action: "delete"`, CORS, `properties` field
